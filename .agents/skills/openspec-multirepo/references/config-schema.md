# Configuration reference

Use strict YAML. Repository names are lowercase and unique; sources are canonical existing Git roots; worktrees remain beneath the task `worktrees/` directory; dependencies resolve and are acyclic. Checks use executable plus argument arrays, never shell strings.
