# Ownership reference

Choose exactly one contract owner for each cross-repository protocol. Downstream repositories depend on that owner and may contain client mappings, UI behavior, or compatibility tests. If ownership is ambiguous, stop before execute and ask the user.
